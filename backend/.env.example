# ============================================
# Environment Variables for Code Library Backend
# ============================================

# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ============================================
# Server Configuration
# ============================================
NODE_ENV=development
PORT=5000

# Frontend URL (for CORS)
# Default: http://localhost:5173 (Vite default)
FRONTEND_URL=http://localhost:5173

# ============================================
# MongoDB Atlas Configuration
# ============================================
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# 
# Steps to get your connection string:
# 1. Go to https://www.mongodb.com/cloud/atlas
# 2. Create a free account or sign in
# 3. Create a new project
# 4. Create a new cluster (free tier available)
# 5. Create a database user with username and password
# 6. Get the connection string from "Connect" button
# 7. Replace <username>, <password>, and <database> with your values
#
# Example: mongodb+srv://admin:mySecurePassword123@mycluster.mongodb.net/codeLibrary?retryWrites=true&w=majority

MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/codeLibrary?retryWrites=true&w=majority

# Database name (used if needed separately)
MONGODB_DB_NAME=codeLibrary

# ============================================
# JWT Configuration
# ============================================
# Secret key for JWT signing (use a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_very_long_secret_key_change_this_in_production_use_32_bytes_of_random_data

# JWT expiration time
JWT_EXPIRATION=7d

# ============================================
# Admin User Configuration (for initial setup)
# ============================================
# These can be used to create the first admin user via admin setup script
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!@#

# ============================================
# Email Configuration (optional for future features)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ============================================
# File Upload Configuration
# ============================================
# Maximum file size in bytes (10MB = 10485760)
MAX_FILE_SIZE=10485760

# ============================================
# Logging Configuration
# ============================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# API Rate Limiting (optional for future)
# ============================================
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
